import { useState } from "react";

const Stores = ({stores}) => {

    

    return (
        <div className="app-container">
            <table>
                <thead>
                    
                </thead>
            </table>
        </div>
    );
}

export default Stores;